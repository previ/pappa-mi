import datetime
from google.appengine.ext import db
from py.model import *

class CommissioneLoader(Loader):
  def __init__(self):
    Loader.__init__(self, 'Commissione',
                    [('nome', str),
                     ('nomeScuola', str),
                     ('strada', str),
                     ('civico', str),
                     ('citta', str),
                     ('cap', str),
                     ('cc', lambda x: db.Key.from_path('CentroCucina',x))
                     ])
import datetime
from google.appengine.ext import db
from model import *

class MenuLoader(Loader):
  def __init__(self):
    Loader.__init__(self, 'Menu',
                    [('validitaDa', str),
                     ('validitaA', str),
                     ('settimana', str),
                     ('giorno', str),
                     ('primo', str),
                     ('secondo', str),
                     ('contorno', str)
                     ])